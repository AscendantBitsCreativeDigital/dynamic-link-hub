=== Dynamic Link Hub ===
Contributors: ascendantbits
Tags: link in bio, link hub, shortcode, social icons, buttons
Requires at least: 5.8
Tested up to: 7.1
Requires PHP: 7.4
Stable tag: 1.4.0
License: GPLv2 or later
License URI: https://www.gnu.org/licenses/gpl-2.0.html

A configurable link-in-bio hub: avatar, latest-post button, custom link
buttons, and social icons, all from one shortcode.

== Description ==

Drop `[dynamic_link_hub]` on any page or post (or in a Shortcode block) to
render a self-contained, centered hub:

1. An optional round avatar image at the top, with configurable size and
   border thickness/color.
2. A button that always links to your most recently published post
   (label and on/off are configurable).
3. Any number of custom buttons you add, each linking to an existing page
   or post, or to a custom URL — add, edit, remove, and drag to reorder
   from **Link Hub** in the admin menu.
4. A horizontally centered row of large, official-brand-color social
   icons below the buttons, built from its own **Social Links** repeater
   — pick a platform (Facebook, Instagram, X/Twitter, Threads, Bluesky,
   YouTube, TikTok, Pinterest, Reddit, LinkedIn, WhatsApp, Telegram,
   Discord, Spotify, SoundCloud, Patreon, Ko-fi, Etsy, email, phone, or
   a custom link), paste the URL/handle/email/number, and add, edit,
   remove, or drag to reorder as many as you want. No WordPress menu
   required. Brand icons and colors are sourced from Simple Icons
   (CC0 1.0 Universal / public domain); LinkedIn uses a plain "in"
   wordmark since LinkedIn had its logo removed from that project.

Button color, text color, hover colors, corner radius, and the overall
container width are all configurable under the **Appearance** tab.

The original shortcode tag (`[dynamic_link_hub_final]`) still works, so
this is a drop-in replacement if you were using the earlier hand-rolled
snippet this plugin was built from.

This plugin is distributed from its own [GitHub repository](https://github.com/AscendantBitsCreativeDigital/dynamic-link-hub)
rather than WordPress.org. It checks that repository for new releases and
shows the normal "Update available" / "Update Now" prompt in wp-admin,
the same as any other plugin — no separate download-and-reinstall needed.

Developed and maintained by [Ascendant Bits Creative Digital](https://ascendantbits.com/).

== Installation ==

1. Upload the `dynamic-link-hub` folder to `/wp-content/plugins/`, or
   install the zip via Plugins → Add New → Upload Plugin.
2. Activate the plugin.
3. Go to **Link Hub** in the admin menu to configure colors, links, and
   social icons.
4. Place `[dynamic_link_hub]` wherever you want the button stack to show.

== Frequently Asked Questions ==

= Does this require a Linktree (or similar) account? =

No. Everything — the avatar, buttons, and social icons — is generated by
this plugin from settings you configure under **Link Hub** in your own
WordPress admin. No third-party account or service is involved.

= Where are my settings stored? =

In a single WordPress option (`dlh_settings`) in your site's own database.
Nothing is sent to any external server.

= What happens to my settings if I uninstall the plugin? =

Deactivating the plugin keeps your settings in case you reactivate it
later. Deleting it from the Plugins screen removes the saved option
entirely.

= Can I use more than one link hub on a site? =

Yes — place the `[dynamic_link_hub]` shortcode on as many pages or posts
as you like; each renders the same configured hub.

= Is the social icon row responsive? =

Yes, the icons wrap onto additional rows on narrow screens automatically.

== Screenshots ==

1. The button hub, avatar, and social icon row as rendered by the shortcode.
2. The Appearance tab: colors, corner radius, container width, and avatar settings.
3. The Links tab: adding and reordering custom buttons.
4. The Social Links tab: adding a platform, handle, or custom link.

== Changelog ==

= 1.4.0 =
* Added self-hosted update checking against this plugin's GitHub
  repository (via the bundled, MIT-licensed Plugin Update Checker
  library), so wp-admin shows "Update available" the normal way. This
  plugin is distributed from GitHub, not WordPress.org.

= 1.3.1 =
* Standards/compliance pass ahead of WordPress.org submission: updated
  "Tested up to", replaced a competitor product name with a generic
  description in the plugin listing, added author/license doc blocks to
  every source file, and tidied the readme (FAQ, screenshots, credits).

= 1.3.0 =
* The whole hub now renders inside its own self-contained container, with
  a configurable width (Appearance tab).
* Added an optional round avatar image above the buttons, with
  configurable image, size, and border thickness/color.

= 1.2.1 =
* Social icons: background is now always transparent (no more colored
  circle); the icon itself lightens on hover/focus instead of the
  background changing color.

= 1.2.0 =
* Social icons now use official brand logos and colors (via Simple
  Icons) instead of plain letter badges, and are noticeably larger.
* Added Bluesky and Reddit to the Social Links platform list.

= 1.1.0 =
* Replaced the WordPress-menu-based social menu with a dedicated Social
  Links repeater (pick a platform, paste a link — no menu needed).

= 1.0.0 =
* Initial release.

== Upgrade Notice ==

= 1.4.0 =
Adds automatic update checking via GitHub. After this version, updates
show up in wp-admin normally instead of needing a manual re-download.

= 1.3.1 =
Compliance/housekeeping release ahead of WordPress.org submission; no
settings changes required.

= 1.3.0 =
Adds a configurable container width and an optional avatar image. No
action needed — existing hubs keep their previous 480px width.
